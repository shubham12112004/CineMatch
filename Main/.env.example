# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# TMDB_API_KEY: Required for movie and series data.
# Get one at https://www.themoviedb.org/documentation/api
TMDB_API_KEY="MY_TMDB_API_KEY"

# MONGODB_URI: MongoDB connection string for user data storage.
# Use local MongoDB: mongodb://localhost:27017/cinematch
# Or MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/cinematch
MONGODB_URI="mongodb://localhost:27017/cinematch"

# JWT_SECRET: Secret key for signing JWT tokens
JWT_SECRET="your_jwt_secret_key_change_this"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# VITE_GOOGLE_AUTH_ENABLED: Enables the Google sign-in button in the frontend.
VITE_GOOGLE_AUTH_ENABLED="false"

# GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET: Google OAuth credentials for backend auth.
# Create them in Google Cloud Console and add an authorized redirect URI for your backend callback.
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
